<?xml version="1.0" encoding="UTF-8"?>
<tomcat-users>
  <!-- Rôles définis -->
  {% for role in tomcat_roles %}
  <role rolename="{{ role }}"/>
  {% endfor %}

  <!-- Utilisateurs définis -->
  {% for user in tomcat_users %}
  <user username="{{ user.username }}" password="{{ user.password }}" roles="{{ user.roles | join(',') }}"/>
  {% endfor %}
</tomcat-users>
